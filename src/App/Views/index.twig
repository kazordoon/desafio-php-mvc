<!DOCTYPE html>
<html lang="pt-br">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Home</title>
  <link rel="stylesheet" href="{{ constant('BASE_URL') }}assets/css/styles.css" />
  <style>
    hr {
      width: 100%;
      margin: 0.5rem 0;
    }
  </style>
</head>

<body>
  <div class="container center">
    <h1 class="title">Olá, {{ user.name }}</h1>
    <p>Seu id: {{ user.id }}</p>
    <hr />
    <p><a href="{{ constant('BASE_URL') }}logout">Sair</a></p>
  </div>
</body>

</html>
